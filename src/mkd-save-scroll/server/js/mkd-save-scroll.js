var mkd = mkd || {};

(function ($, storage, debug) {
  const storagePrefix = "apexSaveScrollPosition";
  const myStorage = apex.storage.getScopedSessionStorage({});
})(apex.jQuery, apex.storage, apex.debug);
